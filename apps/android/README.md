# Android App

Mobile app boundary.

Recommended MVP path:

1. Start with React Native if speed and shared product UI matter most.
2. Use Kotlin later if NFC/passenger-wallet integrations become central.

The Android client should call the same backend API as web and bots.
