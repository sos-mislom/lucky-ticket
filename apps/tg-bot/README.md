# Telegram Bot

Thin bot entrypoint for the backend API.

MVP commands:

- `/start`: link or create account.
- photo/document upload: send ticket image to backend.
- `/top`: show leaderboard.
- `/profile`: show profile card link.

The bot should not contain ticket parsing or scoring logic.
